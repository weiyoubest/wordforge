# 背单词APP — 详细设计方案

> 文档版本：v1.0  
> 日期：2026-07-13  
> 设计师：产品设计师  
> 项目代号：WordForge（暂定）

---

## 目录

- [一、启动页 / 引导页](#一启动页--引导页)
- [二、首页（今日任务 + 快速入口）](#二首页今日任务--快速入口)
- [三、学习页（单词卡片学习流程）](#三学习页单词卡片学习流程)
- [四、复习页（艾宾浩斯复习流程）](#四复习页艾宾浩斯复习流程)
- [五、错词本页（列表 + 管理）](#五错词本页列表--管理)
- [六、词库管理页（内置词库 + 导入）](#六词库管理页内置词库--导入)
- [七、单词详情页（点击单词弹出完整信息）](#七单词详情页点击单词弹出完整信息)
- [八、统计页（数据图表 + 打卡日历）](#八统计数据图表--打卡日历)
- [九、设置页](#九设置页)
- [十、技术架构设计](#十技术架构设计)

---

## 一、启动页 / 引导页

### 1.1 启动页（Splash Screen）

**页面用途**：APP冷启动过渡，展示品牌标识。

#### 布局
```
┌──────────────────────────────┐
│                              │
│                              │
│         [APP Logo]           │  ← 居中，带呼吸缩放微动效
│        "WordForge"           │  ← 品牌名，26sp Medium
│      "科学记忆，高效背词"      │  ← Slogan，12sp Regular，#5D5D5D
│                              │
│                              │
│                              │
│       Loading Indicator       │  ← 底部居中，Material Progress
└──────────────────────────────┘
```

#### 交互行为
- **停留时间**：1.5秒自动跳转
- **首次启动** → 进入引导页
- **非首次启动** → 直接进入首页
- **点击屏幕任意位置** → 跳过动画，立即进入下一页

#### UI组件清单
| 组件 | 类型 | 说明 |
|------|------|------|
| Logo | ImageView | 居中展示，呼吸动画 |
| Slogan | TextView | 品牌标语 |
| ProgressBar | LinearProgressIndicator | 数据库初始化进度（如首次） |

#### 动效说明
- Logo：入场缩放从 0.8→1.0，持续 600ms，ease-out
- Slogan：淡入 + 上移，delay 300ms，持续 400ms
- 整体：1.5s 后淡出过渡到下一页

#### 深色/浅色模式
- 浅色：白色背景 + 深色Logo
- 深色：#121212 背景 + 浅色Logo（反转色调）

#### 异常状态
- **数据库初始化失败**：弹出 Snackbar 提示"初始化失败，请重启应用"，提供"重试"按钮

---

### 1.2 引导页（Onboarding，仅首次启动）

**页面用途**：介绍APP核心功能，降低新手认知成本。

#### 布局
```
┌──────────────────────────────┐
│  [返回箭头] (右上角，可跳过)   │
│                              │
│       [插画/图标]             │  ← 页面主题插图，占屏幕40%
│       标题文字               │  ← 20sp Medium
│       描述文字               │  ← 14sp Regular，2-3行
│                              │
│                              │
│    ● ○ ○                     │  ← 页面指示器（3页）
│  [下一步] / [开始使用]        │  ← 底部按钮
└──────────────────────────────┘
```

#### 三页内容

| 页码 | 图标 | 标题 | 描述 |
|------|------|------|------|
| 第1页 | 📚 图标 | 科学记忆法 | 基于艾宾浩斯遗忘曲线，自动安排复习时间，科学高效 |
| 第2页 | 🧠 图标 | 深度学习 | 词根词缀、近反义词、易混淆词，全方位掌握单词 |
| 第3页 | 🔒 图标 | 纯离线可用 | 无需注册，无需网络，数据安全，随时学习 |

#### 交互行为
- **左右滑动**：切换页面，ViewPager2 实现
- **底部按钮**：第1-2页"下一步"，第3页"开始使用"
- **右上角跳过**：直接进入词库选择页

#### UI组件清单
| 组件 | 类型 | 说明 |
|------|------|------|
| 插图区域 | ImageView / LottieAnimation | 各页主题插图 |
| 标题/描述 | TextView | 文字内容 |
| 页面指示器 | DotsIndicator / TabLayout | 当前页标识 |
| CTA按钮 | FilledButton | 下一步/开始使用 |

#### 动效说明
- 页面切换：水平滑动，ease-in-out，300ms
- 插图：每页切换时轻微缩放+淡入，200ms
- 指示器：平滑过渡，200ms

#### 深色/浅色模式
- 浅色：白色背景，插画保持彩色
- 深色：#121212 背景，插画保持彩色

#### 异常状态
- 无（纯静态引导页）

---

### 1.3 词库选择页（引导流程第4步）

#### 布局
```
┌──────────────────────────────┐
│  ← 选择你的词库               │  ← TopAppBar
│                              │
│  ┌─────────────────────┐    │
│  │ 📗 CET-4      4500词 │    │  ← 可选词库卡片（网格2列）
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ 📘 CET-6      5500词 │    │
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ 📕 考研英语    5500词 │    │
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ 📙 高考英语    3500词 │    │
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ 📓 IELTS      4000词 │    │
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ 📒 TOEFL      4500词 │    │
│  └─────────────────────┘    │
│                              │
│      [确认选择]              │  ← Bottom Button
└──────────────────────────────┘
```

#### 交互行为
- **单选**：点击词库卡片选中（支持多选，至少选1个）
- **确认选择**：进入每日计划设置页
- 选中态：卡片边框变为 Primary 色 + 勾选图标

#### 异常状态
- 无词库可用（极端情况）：提示"词库加载失败，请重启"

---

## 二、首页（今日任务 + 快速入口）

### 2.1 页面布局

```
┌──────────────────────────────┐
│  🔍 搜索单词...               │  ← 顶部搜索栏（OutlinedTextField）
├──────────────────────────────┤
│                              │
│  ┌─────────────────────┐    │
│  │   [环形进度图]        │    │  ← 今日任务卡片
│  │    12/20 新词        │    │     中心显示进度数字
│  │    5/8 复习          │    │
│  │  连续打卡 🔥 15天     │    │
│  └─────────────────────┘    │
│                              │
│  ┌──────┐ ┌──────┐ ┌──────┐│  ← 快速操作（3个等宽按钮）
│  │ 开始  │ │ 开始  │ │ 错词  ││
│  │ 学习  │ │ 复习  │ │ 复习  ││
│  └──────┘ └──────┘ └──────┘│
│                              │
│  📅 今日学习日历（周视图）     │  ← 本周打卡缩略
│  │ 一 二 三 四 五 六 日 │    │
│  │ ✓ ✓ ✓ ✓ ○ ○ ○ │    │
│                              │
│  💡 每日一句                  │  ← 可选模块
│  "The only way to do great   │
│   work is to love what you   │
│   do." — Steve Jobs          │
│                              │
├──────────────────────────────┤
│ [首页] [学习] [复习] [错词本] [我的] │  ← Bottom Nav
└──────────────────────────────┘
```

### 2.2 交互行为

| 操作 | 目标 | 行为 |
|------|------|------|
| 点击搜索栏 | 搜索页 | 进入搜索，自动聚焦输入框 |
| 点击环形进度区域 | 学习页 | 直接跳转学习页 |
| 点击"开始学习" | 学习页 | 跳转学习页，开始今日新词学习 |
| 点击"开始复习" | 复习页 | 跳转复习页 |
| 点击"错词复习" | 错词专项复习 | 直接进入错词复习模式 |
| 下拉首页 | 刷新 | 刷新今日任务数据（SwipeRefreshLayout） |
| 长按每日一句 | 复制 | 复制句子到剪贴板 + Toast 提示 |

### 2.3 UI组件清单

| 组件 | Material 3 类型 | 说明 |
|------|----------------|------|
| 搜索栏 | SearchBar / OutlinedTextField | 带搜索图标，圆角 |
| 今日任务卡片 | Card (Elevated) | 圆角16dp，带阴影 |
| 环形进度 | 自定义 CircularProgressIndicator | 双环（新词+复习），中心文字 |
| 快速操作按钮 | Card (Filled) × 3 | 等宽排列，图标+文字 |
| 日历周视图 | 自定义 Horizontal Calendar | 7天，已打卡绿色✓ |
| 每日一句 | Card (Outlined) | 可选模块 |
| Bottom Nav | NavigationBar | 5个Tab |

### 2.4 动效说明
- **环形进度**：入场时从0度动画填充到当前值，800ms，ease-out
- **快速操作按钮**：依次延迟入场（100ms间隔），从下方淡入上移
- **日历**：打卡日圆点从底部弹入，200ms

### 2.5 深色/浅色模式

| 元素 | 浅色 | 深色 |
|------|------|------|
| 页面背景 | #F8F9FA | #121212 |
| 卡片背景 | #FFFFFF | #1E1E1E |
| 环形进度-新词 | Primary #1A73E8 | Primary #90CAF9 |
| 环形进度-复习 | 学习紫 #7B61FF | #B39DDB |
| 打卡标记 | Success #1E8E3E | #81C995 |
| 操作按钮文字 | #FFFFFF | #FFFFFF |
| 快速操作背景 | Primary #1A73E8 | #1A3A5C |

### 2.6 异常状态

| 状态 | 表现 | 处理 |
|------|------|------|
| **无任务（全部完成）** | 环形进度满圆，中间显示"🎉 今日任务已完成！" | 快速操作按钮变灰/隐藏 |
| **首次使用（未设置词库）** | 卡片区域显示空状态插画 + "请先选择词库" | 点击跳转词库管理 |
| **数据加载中** | 骨架屏（Shimmer） | 环形进度+卡片用 Shimmer 效果 |
| **断卡后首次打开** | 打卡断裂标记，橙色"连续打卡已中断" | 提示但不阻断操作 |

---

## 三、学习页（单词卡片学习流程）

### 3.1 页面布局

```
┌──────────────────────────────┐
│  ← 学习          12 / 20     │  ← TopAppBar，左侧返回，右侧进度
│  ═════════════░░░░░░░░░░    │  ← 线性进度条（60%）
├──────────────────────────────┤
│                              │
│                              │
│         abandon              │  ← 单词（36sp Bold）
│         /əˈbændən/           │  ← 音标（16sp Regular）
│                              │
│      [点击查看释义]           │  ← 提示文字（仅正面显示）
│                              │
│                              │
├──────────────────────────────┤
│                              │  ← 认知标记区域
│  [ 不认识 ] [ 模糊 ] [ 熟悉 ] │     三个等宽按钮，红/橙/绿
└──────────────────────────────┘
```

**翻转后（背面）：**
```
┌──────────────────────────────┐
│  ← 学习          12 / 20     │
│  ═════════════░░░░░░░░░░    │
├──────────────────────────────┤
│         abandon              │  ← 单词
│         /əˈbændən/      🔊  │  ← 发音按钮
│                              │
│  vt. 放弃；抛弃；遗弃          │  ← 中文释义（20sp）
│                              │
│  📖 例句：                   │  ← 例句区域
│  He abandoned his wife and   │
│  children.                   │
│  他抛弃了妻子和孩子。           │
│                              │
│      [ 点击查看更多 ]          │  ← 跳转单词详情
├──────────────────────────────┤
│  [ 不认识 ] [ 模糊 ] [ 熟悉 ] │
└──────────────────────────────┘
```

### 3.2 交互行为

| 操作 | 行为 | 说明 |
|------|------|------|
| 点击卡片正面 | 翻转到背面 | 3D翻转动画 |
| 再次点击背面 | — | 无操作（防误触） |
| 上滑卡片 | 翻转到背面 | 备选手势 |
| 点击 🔊 发音按钮 | 播放美音 | 使用 TTS 或内置音频 |
| 点击"不认识" | 标记+下一词 | 卡片向左滑出，红色闪光 |
| 点击"模糊" | 标记+下一词 | 卡片向下滑出，橙色闪光 |
| 点击"熟悉" | 标记+下一词 | 卡片向右滑出，绿色闪光 |
| 点击"点击查看更多" | 单词详情 | 打开单词详情 BottomSheet |
| 左右滑动卡片 | — | 锁定（防误操作），学习模式不支持滑动切词 |

### 3.3 认知标记与调度策略

| 标记 | 颜色 | 含义 | 调度结果 |
|------|------|------|---------|
| **熟悉** | 绿 #1E8E3E | 已掌握 | 正常进入遗忘曲线（5min→30min→...） |
| **模糊** | 橙 #F9AB00 | 有印象但不牢 | 回退到上一次复习间隔（如从30min退回5min） |
| **不认识** | 红 #D93025 | 完全不认识 | 回退到最短间隔（5min），同时加入错词本 |

### 3.4 UI组件清单

| 组件 | 类型 | 说明 |
|------|------|------|
| TopAppBar | TopAppBar | 返回+标题+进度 |
| 进度条 | LinearProgressIndicator | 线性，Primary色 |
| 单词卡片 | 自定义 Card | 可翻转（动画） |
| 发音按钮 | IconButton | 🔊 图标，点击播放 |
| 认知标记按钮 | FilledTonalButton × 3 | 红/橙/绿，等宽 |
| 例句区域 | TextView × 2 | 英文+中文翻译 |

### 3.5 动效说明

| 动效 | 参数 | 说明 |
|------|------|------|
| 卡片翻转 | 400ms, ease-in-out | 3D绕X轴翻转，带阴影变化 |
| 卡片滑出（熟悉） | 300ms, ease-in | 向右滑出+淡出 |
| 卡片滑出（模糊） | 300ms, ease-in | 向下滑出+淡出 |
| 卡片滑出（不认识）| 300ms, ease-in | 向左滑出+淡出 |
| 新卡片入场 | 250ms, ease-out | 从下方淡入上移 |
| 发音按钮 | 150ms | 按压缩小→释放恢复 |
| 背景闪光 | 200ms | 标记时背景短暂闪红/橙/绿 |

### 3.6 深色/浅色模式

| 元素 | 浅色 | 深色 |
|------|------|------|
| 卡片背景 | #FFFFFF | #1E1E1E |
| 单词文字 | #1F1F1F | #E0E0E0 |
| 音标文字 | #5D5D5D | #9E9E9E |
| 释义文字 | #1F1F1F | #E0E0E0 |
| 例句英文 | #1F1F1F | #E0E0E0 |
| 例句中文 | #5D5D5D | #9E9E9E |

### 3.7 异常状态

| 状态 | 表现 | 处理 |
|------|------|------|
| **今日新词已学完** | 卡片区域显示"🎉 今日新词已全部学习！" + 确认按钮 | 点击返回首页 |
| **词库为空** | 空状态插画 + "当前词库没有单词" | 提供"切换词库"按钮 |
| **发音失败** | Toast"发音加载失败" | 不阻断学习流程 |
| **学习中断后回来** | 提示"从第 X 个继续？" + 确认 | 跳转到上次位置 |

---

## 四、复习页（艾宾浩斯复习流程）

### 4.1 页面布局

**复习列表视图：**
```
┌──────────────────────────────┐
│  复习           共 23 词待复习 │  ← TopAppBar
├──────────────────────────────┤
│                              │
│  ⚠️ 有 5 个错词需要复习！     │  ← 错词优先提示（如有）
│  [ 立即复习错词 ]             │  ← 醒目按钮（Error Container）
│                              │
│  ── 今日到期 ──               │
│  ┌─────────────────────┐    │
│  │ 🕐 5分钟     3词     │    │  ← 复习时间分组卡片
│  │ 🕐 30分钟    5词     │    │
│  │ 🕐 12小时    8词     │    │
│  └─────────────────────┘    │
│                              │
│  ── 明日到期 ──               │
│  ┌─────────────────────┐    │
│  │ 📅 1天后     12词    │    │
│  │ 📅 2天后     10词    │    │
│  └─────────────────────┘    │
│                              │
│  ── 本周到期 ──               │
│  ┌─────────────────────┐    │
│  │ 📅 4天后     8词     │    │
│  │ 📅 7天后     15词    │    │
│  │ 📅 15天后    20词    │    │
│  └─────────────────────┘    │
│                              │
│      [ 开始全部复习 ]         │  ← 底部 FAB / 按钮
│                              │
├──────────────────────────────┤
│ [首页] [学习] [复习] [错词本] [我的] │
└──────────────────────────────┘
```

**复习卡片视图**：与学习页卡片布局相同（复用组件），但顶部显示"复习模式"。

### 4.2 交互行为

| 操作 | 行为 |
|------|------|
| 点击时间分组卡片 | 展开显示该时段待复习单词列表 |
| 点击"立即复习错词" | 进入错词专项复习模式（优先级最高） |
| 点击"开始全部复习" | 从最早到期开始，逐个复习 |
| 复习卡片交互 | 与学习页完全一致（翻转+认知标记） |
| 复习时标记"熟悉" | 移出当前复习队列，更新下次复习时间 |
| 复习时标记"不认识" | 回退间隔 + 加入错词本 |

### 4.3 UI组件清单

| 组件 | 类型 | 说明 |
|------|------|------|
| 错词优先提示 | Card (Error Container) | 红色背景，醒目 |
| 时间分组卡片 | Card (Outlined) | 按时间段分组展示 |
| 开始复习按钮 | FilledButton | Primary色，底部固定 |
| 复习卡片 | 复用学习页卡片组件 | 完全复用 |

### 4.4 动效说明
- 时间分组卡片：入场依次从下方淡入，100ms间隔
- 复习卡片动效：复用学习页动效规范
- 复习完成：弹出完成总结对话框，confetti粒子效果（轻量）

### 4.5 深色/浅色模式

| 元素 | 浅色 | 深色 |
|------|------|------|
| 错词提示背景 | #FDEAEA (Error Container) | #3C1010 |
| 分组卡片 | #FFFFFF | #1E1E1E |
| 到期数量 | Primary #1A73E8 | #90CAF9 |

### 4.6 异常状态

| 状态 | 表现 |
|------|------|
| **无待复习单词** | 空状态插画 + "暂无待复习单词，继续保持！" + 返回首页按钮 |
| **只有错词待复习** | 直接显示错词复习入口，隐藏普通复习分组 |

---

## 五、错词本页（列表 + 管理）

### 5.1 页面布局

```
┌──────────────────────────────┐
│  错词本          🔍 ☰          │  ← TopAppBar，搜索+菜单
├──────────────────────────────┤
│  共 42 个错词                  │  ← 统计栏
│  [全部] [按日期] [按词库] [按次数]│  ← 筛选Tab（ChipGroup）
├──────────────────────────────┤
│  ┌─────────────────────┐    │
│  │ abandon    v.放弃     │    │  ← 错词列表项
│  │ 错误 2次  CET-4      │    │
│  │ [复习] [移除]         │    │
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ elaborate  adj.精心  │    │
│  │ 错误 1次  CET-6      │    │
│  │ [复习] [移除]         │    │
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ ...更多错词...         │    │
│  └─────────────────────┘    │
│                              │
├──────────────────────────────┤
│ [首页] [学习] [复习] [错词本] [我的] │
└──────────────────────────────┘
```

### 5.2 交互行为

| 操作 | 行为 |
|------|------|
| 点击错词项 | 弹出单词详情 BottomSheet |
| 点击"复习" | 将该词加入临时复习队列（可批量） |
| 点击"移除" | 从错词本移除（需确认对话框） |
| 长按错词项 | 进入多选模式 |
| 多选模式下 | 顶部显示"已选X个"+ 批量移除/批量复习 |
| 点击筛选Tab | 切换排序/筛选方式 |
| 点击搜索图标 | 进入搜索模式，输入即搜 |
| 点击菜单 | 展开选项：错词专项复习 / 导出错词 / 清空错词本 |

### 5.3 筛选/排序方式

| 筛选维度 | 排序选项 |
|---------|---------|
| 按日期 | 最新加入 ↑ / 最早加入 ↓ |
| 按词库 | 按词库分组（CET-4、CET-6...） |
| 按次数 | 错误次数 ↑ / ↓ |

### 5.4 UI组件清单

| 组件 | 类型 | 说明 |
|------|------|------|
| 错词列表项 | ListItem (三行) | 单词+释义+元信息 |
| 筛选Tab | FilterChip × 4 | 单选切换 |
| 快速操作按钮 | OutlinedButton × 2 | 复习/移除 |
| 搜索图标 | IconButton | 顶部搜索 |
| 菜单图标 | IconButton | 更多操作 |

### 5.5 动效说明
- 列表项入场：Staggered 从下方淡入，每项延迟 50ms
- 删除动画：列表项高度收缩+淡出，300ms
- 筛选切换：列表淡出 → 筛选 → 淡入，200ms

### 5.6 深色/浅色模式

| 元素 | 浅色 | 深色 |
|------|------|------|
| 列表项背景 | #FFFFFF | #1E1E1E |
| 错误次数文字 | #D93025 | #F28B82 |
| 操作按钮 | Primary outline | Primary outline |

### 5.7 异常状态

| 状态 | 表现 |
|------|------|
| **错词本为空** | 空状态插画 + "暂无错词，继续保持！" + 返回首页按钮 |
| **搜索无结果** | "未找到匹配的单词" + 清除搜索按钮 |

---

## 六、词库管理页（内置词库 + 导入）

### 6.1 页面布局

```
┌──────────────────────────────┐
│  ← 词库管理                   │  ← TopAppBar
├──────────────────────────────┤
│                              │
│  当前使用：CET-4 (4500词)     │  ← 当前词库提示
│  已学 320 / 4500，进度 7%     │
│                              │
│  ── 内置词库 ──               │
│  ┌─────────────────────┐    │
│  │ 📗 CET-4     4500词  │ ✓ │  ← 当前使用，带勾
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ 📘 CET-6     5500词  │ + │  ← 点击切换/添加
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ 📕 考研英语   5500词  │ + │
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ 📙 高考英语   3500词  │ + │
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ 📓 IELTS     4000词  │ + │
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ 📒 TOEFL     4500词  │ + │
│  └─────────────────────┘    │
│                              │
│  ── 自定义词库 ──             │
│  ┌─────────────────────┐    │
│  │ 📁 我的词库1  200词  │ ✕ │  ← 可删除
│  └─────────────────────┘    │
│                              │
│  [+ 导入词库]                │  ← FAB 或按钮
│                              │
└──────────────────────────────┘
```

### 6.2 交互行为

| 操作 | 行为 |
|------|------|
| 点击内置词库(+号) | 切换为当前词库，或添加到学习列表 |
| 点击已使用的词库(✓) | 显示详情（词汇量、已学进度） |
| 点击自定义词库(✕) | 确认删除对话框 → 从数据库移除 |
| 点击"导入词库" | 打开文件选择器（CSV/JSON/TXT） |
| 长按自定义词库 | 显示重命名/删除选项 |

### 6.3 导入词库流程

```
点击导入 → 文件选择器 → 选择文件 → 解析预览
→ 显示：文件名、单词数量、预览前10个
→ [确认导入] / [取消]
→ 导入中（进度条）→ 完成/失败提示
```

### 6.4 支持的文件格式

| 格式 | 说明 | 示例 |
|------|------|------|
| **CSV** | 英文,中文释义,音标(可选) | `abandon,放弃,əˈbændən` |
| **JSON** | 数组格式，含word/meaning/phonetic | `[{"word":"abandon","meaning":"放弃"}]` |
| **TXT** | 每行一个"英文 中文"对 | `abandon 放弃` |

### 6.5 UI组件清单

| 组件 | 类型 | 说明 |
|------|------|------|
| 词库列表项 | ListItem (两行) | 词库名+词汇量+状态图标 |
| 当前词库卡片 | Card (Primary Container) | 蓝色背景，展示进度 |
| 导入按钮 | FAB / FilledButton | Primary色 |
| 文件选择器 | Activity Result Launcher | 系统文件选择 |

### 6.6 动效说明
- 列表项切换状态：勾选图标缩放弹入，200ms
- 导入进度：CircularProgressIndicator + 百分比数字

### 6.7 异常状态

| 状态 | 表现 |
|------|------|
| **文件解析失败** | 对话框提示错误原因（格式错误/编码问题）+ 重试 |
| **文件为空** | Toast"文件中没有找到单词" |
| **导入重复词库** | 对话框"已存在同名词库，是否覆盖？" |

---

## 七、单词详情页（点击单词弹出完整信息）

### 7.1 页面布局（BottomSheet / 全屏弹层）

```
┌──────────────────────────────┐
│  ═══════ (拖拽条)             │  ← 顶部拖拽指示器
│  abandon          ⭐ 📕     │  ← 单词 + 收藏/加入错词本按钮
│  /əˈbændən/           🔊    │  ← 音标 + 发音按钮
├──────────────────────────────┤
│  vt. 放弃；抛弃；遗弃          │  ← 中文释义
│  vi. 放弃                    │  ← 多个词性释义分行
├──────────────────────────────┤
│  📖 例句                     │
│  ┌─────────────────────┐    │
│  │ He abandoned his    │    │
│  │ wife and children.  │    │  ← 英文例句
│  │ 他抛弃了妻子和孩子。   │    │  ← 中文翻译
│  └─────────────────────┘    │
├──────────────────────────────┤
│  🌳 词根词缀                  │
│  a-(加强) + bandon(控制)      │
│  → 加强控制 → 放弃控制        │  ← 联想记忆说明
├──────────────────────────────┤
│  🔗 近义词 / 反义词           │
│  近义词：forsake, desert,     │
│         relinquish, quit     │
│  反义词：retain, keep, claim │
├──────────────────────────────┤
│  ⚠️ 易混淆词                  │
│  ┌─────────────────────┐    │
│  │ abandon vs. desert   │    │
│  │ abandon：主动放弃      │    │
│  │ desert：遗弃（含贬义） │    │
│  └─────────────────────┘    │
└──────────────────────────────┘
```

### 7.2 交互行为

| 操作 | 行为 |
|------|------|
| 点击 🔊 | 播放美音发音 |
| 点击 ⭐ 收藏 | 切换收藏状态（实心/空心） |
| 点击 📕 错词本 | 添加到/从错词本移除 |
| 点击近义词/反义词中的单词 | 跳转到该词的单词详情（替换当前内容） |
| 下拉拖拽条 | 关闭 BottomSheet |
| 点击蒙层 | 关闭 BottomSheet |
| 上滑拖拽条 | 展开为全屏模式（查看更多内容时） |

### 7.3 UI组件清单

| 组件 | 类型 | 说明 |
|------|------|------|
| BottomSheet | ModalBottomSheet | 可拖拽展开/关闭 |
| 单词标题 | Text (headline) | 28sp Bold |
| 音标 | Text (subtitle) | 16sp Regular |
| 发音按钮 | IconButton | 🔊 图标 |
| 收藏/错词按钮 | IconButton × 2 | ⭐ / 📕 图标 |
| 各信息区块 | Card / Section | 分区展示，带图标标题 |
| 易混淆词对比 | Card (Outlined) | 两列对比展示 |

### 7.4 动效说明
- BottomSheet 展开：从底部上滑，300ms，ease-out
- BottomSheet 关闭：下滑收起，250ms
- 收藏按钮：心跳缩放动效（❤️ 弹跳），300ms
- 发音按钮：按压缩小，150ms
- 近义词跳转：内容淡出→淡入，200ms

### 7.5 深色/浅色模式

| 元素 | 浅色 | 深色 |
|------|------|------|
| Sheet 背景 | #FFFFFF | #1E1E1E |
| 分隔线 | #E0E0E0 | #3C3C3C |
| 联想记忆文字 | #1A73E8 (链接色) | #90CAF9 |
| 易混淆词卡片 | #F1F3F4 | #2C2C2C |
| 各区块标题图标 | 对应语义色 | 同上（亮度调低） |

### 7.6 异常状态

| 状态 | 表现 |
|------|------|
| **数据不完整**（无词根/例句等） | 对应区块显示"暂无数据"灰色提示，不隐藏区块 |
| **发音失败** | Toast"发音播放失败" |
| **词库中无近义词数据** | 显示"暂无近义词数据" |

---

## 八、统计页（数据图表 + 打卡日历）

### 8.1 页面布局

```
┌──────────────────────────────┐
│  ← 学习统计                   │  ← TopAppBar
├──────────────────────────────┤
│                              │
│  ┌────────┐ ┌────────┐      │
│  │  45    │ │ 1,280  │      │  ← 总览数据卡片
│  │ 学习天数│ │ 累计词汇│      │
│  └────────┘ └────────┘      │
│  ┌────────┐ ┌────────┐      │
│  │  87%   │ │ 🔥 15  │      │
│  │ 正确率  │ │ 连续天数│      │
│  └────────┘ └────────┘      │
│                              │
│  ── 记忆曲线 ──               │
│  ┌─────────────────────┐    │
│  │    📈 折线图          │    │  ← 记忆保持率趋势
│  │    (近30天)          │    │
│  └─────────────────────┘    │
│                              │
│  ── 每日学习量 ──              │
│  ┌─────────────────────┐    │
│  │    📊 柱状图          │    │  ← 每日新学+复习数量
│  │    (近7天)           │    │
│  └─────────────────────┘    │
│                              │
│  ── 打卡日历 ──               │
│  ┌─────────────────────┐    │
│  │   7月 2026            │    │  ← GitHub风格热力图
│  │   ■ ■ ■ □ ■ ■ □      │    │  ■ = 已打卡 □ = 未打卡
│  │   ■ ■ ■ ■ ■ □ □      │    │  颜色深浅表示学习量
│  │   ...                  │    │
│  └─────────────────────┘    │
│                              │
└──────────────────────────────┘
```

### 8.2 交互行为

| 操作 | 行为 |
|------|------|
| 点击总览数据卡片 | 无特殊操作（仅展示） |
| 点击折线图数据点 | 显示该日详细数据 Tooltip |
| 点击柱状图柱体 | 显示该日新学/复习数量 |
| 点击日历日期 | 显示该日学习详情（新学/复习/正确率） |
| 左右滑动图表区域 | 切换时间范围（7天/30天/全部） |

### 8.3 图表规范

| 图表类型 | 数据维度 | 时间范围 | 配色 |
|---------|---------|---------|------|
| 折线图 | 记忆保持率 | 近30天 | Primary 蓝 #1A73E8 |
| 柱状图 | 每日新学(蓝) + 复习(紫) | 近7天 | Primary + 学习紫 |
| 热力图 | 打卡情况 + 学习量 | 当月/近3月 | 绿色梯度（浅→深） |

### 8.4 UI组件清单

| 组件 | 类型 | 说明 |
|------|------|------|
| 总览卡片 | Card × 4 (网格) | 2×2 网格展示 |
| 折线图 | MPAndroidChart / 自定义View | 近30天记忆曲线 |
| 柱状图 | MPAndroidChart / 自定义View | 近7天学习量 |
| 热力日历 | 自定义 CalendarView | GitHub风格 |

### 8.5 动效说明
- 总览数字：数字滚动动画（从0计数到目标值），800ms
- 图表入场：从左到右绘制动画，500ms
- 日历格子：逐格淡入，Staggered 30ms/格

### 8.6 深色/浅色模式

| 元素 | 浅色 | 深色 |
|------|------|------|
| 页面背景 | #F8F9FA | #121212 |
| 卡片背景 | #FFFFFF | #1E1E1E |
| 数字 | #1F1F1F | #E0E0E0 |
| 折线图线 | #1A73E8 | #90CAF9 |
| 柱状图-新学 | #1A73E8 | #90CAF9 |
| 柱状图-复习 | #7B61FF | #B39DDB |
| 热力图-已打卡 | #1E8E3E 渐变 | #81C995 渐变 |
| 热力图-未打卡 | #E8F5E9 | #1B2E1B |

### 8.7 异常状态

| 状态 | 表现 |
|------|------|
| **无学习数据** | 总览卡片全显示 0 / 图表区域显示空状态 + "开始学习后这里会展示你的数据" |
| **数据加载失败** | Toast"数据加载失败" + 重试按钮 |

---

## 九、设置页

### 9.1 页面布局

```
┌──────────────────────────────┐
│  ← 设置                       │  ← TopAppBar
├──────────────────────────────┤
│                              │
│  ── 学习计划 ──               │
│  每日新词数量        [20]     │  ← 点击弹出Slider/NumberPicker
│  目标天数           [30]     │
│                              │
│  ── 外观 ──                   │
│  深色模式      [开关]         │  ← Switch，支持跟随系统
│                              │
│  ── 发音 ──                   │
│  自动播放发音      [开关]     │  ← Switch
│  发音速度          [正常 ▼]   │  ← Dropdown（慢/正常/快）
│                              │
│  ── 复习提醒 ──               │
│  启用复习提醒      [开关]     │  ← Switch
│  提醒时间          [09:00]   │  ← 点击弹出TimePicker
│                              │
│  ── 数据 ──                   │
│  备份数据          [导出]     │  ← 导出为JSON
│  恢复数据          [导入]     │  ← 从JSON恢复
│  清除学习数据      [清除]     │  ← 需二次确认，Error色
│                              │
│  ── 关于 ──                   │
│  版本             v1.0.0      │
│  隐私政策          [查看]     │
│  开源许可          [查看]     │
│                              │
└──────────────────────────────┘
```

### 9.2 交互行为

| 操作 | 行为 |
|------|------|
| 点击"每日新词数量" | 弹出 Slider 对话框（5-100，步长5） |
| 点击"目标天数" | 弹出 Slider 对话框（7-365） |
| 切换深色模式 | 即时切换（支持：关闭/开启/跟随系统） |
| 切换自动播放发音 | 即时生效 |
| 点击发音速度 | Dropdown 选择 |
| 点击提醒时间 | TimePicker 弹出 |
| 点击"备份数据" | 生成 JSON → 系统分享/保存对话框 |
| 点击"恢复数据" | 文件选择器 → 解析 → 确认覆盖对话框 |
| 点击"清除学习数据" | 二次确认对话框 → 清除所有学习记录（保留词库） |

### 9.3 UI组件清单

| 组件 | 类型 | 说明 |
|------|------|------|
| 设置分组标题 | Text (Label) | 灰色小字分区标题 |
| 设置项 | ListItem (两行) | 标题 + 右侧控件 |
| Switch | Switch | 开关控件 |
| Slider | Slider | 数值选择 |
| TimePicker | TimePickerDialog | 时间选择 |

### 9.4 深色/浅色模式
- 设置页自身遵循深色/浅色模式规范
- "深色模式"选项提供三档：关闭 / 开启 / 跟随系统

### 9.5 异常状态

| 状态 | 表现 |
|------|------|
| **备份文件写入失败** | Toast"备份失败，请检查存储权限" |
| **恢复数据格式错误** | 对话框"数据格式不正确，无法恢复" |
| **清除数据确认** | 对话框"确定要清除所有学习数据吗？此操作不可撤销。" + [取消] [确定(红)] |

---

## 十、技术架构设计

### 10.1 整体架构

```
┌──────────────────────────────────────┐
│            UI Layer                   │
│  Activity / Fragment / Compose        │
│  (Material Design 3 Components)      │
├──────────────────────────────────────┤
│          ViewModel Layer              │
│  (StateFlow / LiveData)               │
│  - HomeViewModel                      │
│  - LearnViewModel                     │
│  - ReviewViewModel                    │
│  - WordDetailViewModel                │
│  - StatsViewModel                     │
│  - SettingsViewModel                  │
├──────────────────────────────────────┤
│         Repository Layer              │
│  - WordRepository                     │
│  - LearningRecordRepository            │
│  - MistakeBookRepository              │
│  - WordbookRepository                 │
│  - ReviewScheduleRepository           │
├──────────────────────────────────────┤
│          Data Layer (Room)             │
│  - WordBookDao / WordDao              │
│  - LearningRecordDao                  │
│  - MistakeWordDao                     │
│  - ReviewQueueDao                     │
├──────────────────────────────────────┤
│        Utility / Service Layer        │
│  - ReviewScheduler (遗忘曲线算法)       │
│  - WordImporter (词库导入/解析)         │
│  - AudioPlayer (发音播放)              │
│  - StatsCalculator (统计计算)          │
└──────────────────────────────────────┘
```

### 10.2 数据库表结构设计（Room Entity）

#### Entity 1: `word` — 单词表

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long (PK, auto) | 主键 |
| spelling | String (NOT NULL) | 英文单词 |
| phonetic | String? | 音标 |
| meaning | String (NOT NULL) | 中文释义（支持多行，用\n分隔） |
| partOfSpeech | String? | 词性（v./n./adj.等） |
| exampleSentence | String? | 英文例句 |
| exampleTranslation | String? | 例句中文翻译 |
| rootAffix | String? | 词根词缀联想说明 |
| synonyms | String? | 近义词（逗号分隔） |
| antonyms | String? | 反义词（逗号分隔） |
| confusableWords | String? | 易混淆词（JSON格式，含对比说明） |
| audioPath | String? | 本地音频文件路径（可选） |
| wordbookId | Long (FK) | 所属词库ID |
| difficulty | Int? | 难度等级（1-5） |

**索引**：`idx_word_spelling` (spelling), `idx_word_wordbook` (wordbookId)

#### Entity 2: `wordbook` — 词库表

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long (PK, auto) | 主键 |
| name | String (NOT NULL) | 词库名称（如"CET-4"） |
| description | String? | 词库描述 |
| totalWords | Int | 总词汇量 |
| type | String (NOT NULL) | 词库类型：`builtin` / `custom` |
| isActive | Boolean | 是否为当前学习词库 |
| createdAt | Long | 创建时间戳 |

#### Entity 3: `learning_record` — 学习记录表

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long (PK, auto) | 主键 |
| wordId | Long (FK, NOT NULL) | 关联单词ID |
| recordType | String (NOT NULL) | 记录类型：`new` / `review` / `mistake_review` |
| cognitionLevel | String (NOT NULL) | 认知标记：`familiar` / `vague` / `unknown` |
| reviewedAt | Long (NOT NULL) | 学习/复习时间戳 |
| nextReviewAt | Long | 下次复习时间戳（由算法计算） |
| reviewStage | Int | 当前复习阶段（0-7，对应遗忘曲线各节点） |
| wordbookId | Long (FK) | 当时所属词库 |
| sessionDate | String | 学习日期（yyyy-MM-dd，用于统计） |

**索引**：`idx_record_word` (wordId), `idx_record_next_review` (nextReviewAt), `idx_record_date` (sessionDate)

#### Entity 4: `mistake_word` — 错词表

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long (PK, auto) | 主键 |
| wordId | Long (FK, UNIQUE, NOT NULL) | 关联单词ID（唯一） |
| errorCount | Int (DEFAULT 1) | 累计错误次数 |
| firstErrorAt | Long | 首次错误时间戳 |
| lastErrorAt | Long | 最近错误时间戳 |
| isResolved | Boolean (DEFAULT false) | 是否已掌握（标记熟悉后自动移除） |
| wordbookId | Long (FK) | 来源词库ID |

**索引**：`idx_mistake_word` (wordId UNIQUE)

#### Entity 5: `daily_stats` — 每日统计表

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long (PK, auto) | 主键 |
| date | String (UNIQUE, NOT NULL) | 日期（yyyy-MM-dd） |
| newWordsLearned | Int (DEFAULT 0) | 当日新学单词数 |
| wordsReviewed | Int (DEFAULT 0) | 当日复习单词数 |
| correctCount | Int (DEFAULT 0) | 当日正确数 |
| totalAttempts | Int (DEFAULT 0) | 当日总尝试数 |
| accuracyRate | Float | 当日正确率 |
| streakDays | Int (DEFAULT 0) | 连续打卡天数 |
| studyDurationSec | Int (DEFAULT 0) | 学习时长（秒） |

**索引**：`idx_stats_date` (date UNIQUE)

#### Entity 6: `favorite_word` — 收藏/生词本

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long (PK, auto) | 主键 |
| wordId | Long (FK, UNIQUE, NOT NULL) | 关联单词ID |
| favoritedAt | Long | 收藏时间戳 |

#### Entity 7: `app_settings` — 应用设置

| 字段名 | 类型 | 说明 |
|--------|------|------|
| key | String (PK, NOT NULL) | 设置键 |
| value | String (NOT NULL) | 设置值（JSON序列化） |

---

### 10.3 Repository / ViewModel 层设计

#### Repository 职责

| Repository | 职责 |
|-----------|------|
| **WordRepository** | 单词CRUD、按词库查询、搜索单词、批量导入 |
| **LearningRecordRepository** | 记录学习/复习行为、查询今日进度、查询待复习列表 |
| **MistakeBookRepository** | 错词CRUD、筛选排序、批量操作、统计错误次数 |
| **WordbookRepository** | 词库管理、切换当前词库、获取词库信息 |
| **ReviewScheduleRepository** | 复习队列管理、获取各时段待复习数量、更新下次复习时间 |
| **DailyStatsRepository** | 每日统计读写、查询打卡记录、计算连续天数 |
| **FavoriteRepository** | 收藏管理、生词本查询 |

#### ViewModel 设计

| ViewModel | 数据源 | 暴露的 State |
|-----------|--------|-------------|
| **HomeViewModel** | LearningRecordRepo + DailyStatsRepo + ReviewScheduleRepo | 今日进度(新词/复习数)、连续打卡天数、待复习总数 |
| **LearnViewModel** | WordRepo + LearningRecordRepo + MistakeBookRepo | 当前单词、学习进度、认知标记结果 |
| **ReviewViewModel** | ReviewScheduleRepo + LearningRecordRepo | 待复习列表(按时间分组)、当前复习单词、复习进度 |
| **WordDetailViewModel** | WordRepo + LearningRecordRepo + FavoriteRepo | 单词完整信息(释义/音标/例句/词根/近反义/易混淆)、收藏状态 |
| **StatsViewModel** | DailyStatsRepo + LearningRecordRepo | 总览数据、记忆曲线数据、每日学习量、打卡日历 |
| **SettingsViewModel** | AppSettings (DataStore) | 各设置项值 |

---

### 10.4 复习算法模块设计

#### 艾宾浩斯遗忘曲线调度器（ReviewScheduler）

**核心机制**：基于 5 阶段复习间隔 + 三档认知标记。

**复习间隔表**：

| 阶段 | 间隔 | 累计时间 | 说明 |
|------|------|---------|------|
| Stage 0 | 5分钟 | 5min | 学习后立即首次复习 |
| Stage 1 | 30分钟 | 35min | 第二次复习 |
| Stage 2 | 12小时 | 12h35min | 半天后 |
| Stage 3 | 1天 | 1d12h35min | 次日 |
| Stage 4 | 2天 | 3d12h35min | 第三天 |
| Stage 5 | 4天 | 7d12h35min | 一周后 |
| Stage 6 | 7天 | 14d12h35min | 两周后 |
| Stage 7 | 15天 | 29d12h35min | 一个月后 |
| Stage 8 | — | 标记为"已掌握" | 从复习队列移除 |

**认知标记处理逻辑**：

```
function processCognition(word, level):
    switch(level):
        case "familiar":
            stage = word.stage + 1
            if stage > 8:
                markAsMastered(word)
            else:
                word.nextReviewAt = now + INTERVALS[stage]
                word.stage = stage
        case "vague":
            // 回退到上一阶段，不低于Stage 0
            stage = max(word.stage - 1, 0)
            word.nextReviewAt = now + INTERVALS[stage]
            word.stage = stage
        case "unknown":
            // 回退到最早阶段
            word.stage = 0
            word.nextReviewAt = now + INTERVALS[0]
            addToMistakeBook(word)
```

**错词优先机制**：
- 错词本中的单词具有**最高复习优先级**
- 复习页顶部醒目提示错词数量
- 点击后优先进入错词复习模式
- 错词复习时标记"熟悉" → 自动从错词本移除

**队列排序规则**：
```
优先级排序：
1. 错词本中 nextReviewAt <= now 的单词
2. 普通单词中 nextReviewAt <= now 的单词，按 nextReviewAt 升序
3. 已过期的按过期时长排序（过期越久越优先）
```

---

### 10.5 词库导入/解析模块设计

#### WordImporter 模块

**职责**：解析外部文件 → 标准化数据结构 → 批量写入数据库。

**架构**：

```
WordImporter
├── detectFormat(file) → Format (CSV / JSON / TXT)
├── parseCSV(inputStream) → List<WordImportItem>
├── parseJSON(inputStream) → List<WordImportItem>
├── parseTXT(inputStream) → List<WordImportItem>
├── validate(items) → ValidationResult
│   ├── 格式校验（必填字段）
│   ├── 去重（跳过已存在的单词）
│   └── 编码检测（UTF-8 / GBK）
├── preview(items, limit=10) → List<WordImportItem>  // 预览
└── import(items, wordbookName) → ImportResult
    ├── 批量插入（Room @Transaction）
    └── 返回成功数/失败数/跳过数
```

**WordImportItem 数据结构**：
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| spelling | String | ✅ | 英文单词 |
| meaning | String | ✅ | 中文释义 |
| phonetic | String | ❌ | 音标 |
| exampleSentence | String | ❌ | 例句 |
| exampleTranslation | String | ❌ | 例句翻译 |

**编码检测**：优先 UTF-8，失败后尝试 GBK，确保中文词库兼容。

**批量插入策略**：使用 Room `@Insert` + `@Transaction`，每批 500 条提交，避免大事务阻塞 UI。

---

### 10.6 发音播放模块设计

#### AudioPlayer 模块

**职责**：播放单词美音发音。

**发音来源（优先级）**：

1. **内置音频文件**：词库包附带预录 MP3（如 `audio/abandon.mp3`），最优先使用
2. **Android TTS（TextToSpeech）**：无内置音频时，使用系统 TTS 引擎（美式英语）
3. **静默降级**：TTS 不可用时，Toast 提示"当前设备不支持发音"，不阻断学习

**TTS 配置**：
| 参数 | 值 | 说明 |
|------|-----|------|
| Language | `en-US` | 美式英语 |
| Speed | 0.9x | 略慢，便于听清 |
| Pitch | 1.0 | 正常音调 |
| Engine | Google TTS (优先) | 兼容性最好 |

**缓存策略**：
- TTS 合成的音频缓存到 `cache/audio/` 目录
- 文件名：单词拼写 `.wav`
- 缓存上限：50MB，LRU 淘汰

**播放管理**：
- 单例管理 MediaPlayer
- 新播放请求到来时，停止当前播放
- 播放完毕自动释放资源

---

### 10.7 依赖关系（关键第三方库）

| 库 | 用途 | 版本要求 |
|-----|------|---------|
| Room | SQLite ORM | 2.6.x |
| Material Design 3 | UI组件 | 1.3.x |
| Navigation Component | 页面导航 | 2.8.x |
| ViewModel + LiveData | MVVM架构 | 2.8.x |
| Coroutines / Flow | 异步处理 | 1.8.x |
| DataStore | 设置存储 | 1.1.x |
| MPAndroidChart (可选) | 图表 | 3.1.x |
| Coil (可选) | 图片加载 | 2.7.x |

---

### 10.8 项目目录结构建议

```
app/src/main/
├── java/com/wordforge/app/
│   ├── data/
│   │   ├── db/
│   │   │   ├── AppDatabase.kt
│   │   │   ├── entity/          (Word, Wordbook, LearningRecord...)
│   │   │   ├── dao/             (WordDao, WordbookDao...)
│   │   │   └── converter/       (TypeConverter)
│   │   ├── repository/
│   │   │   ├── WordRepository.kt
│   │   │   ├── LearningRecordRepository.kt
│   │   │   └── ...
│   │   └── importer/
│   │       └── WordImporter.kt
│   ├── ui/
│   │   ├── home/
│   │   │   ├── HomeFragment.kt
│   │   │   ├── HomeViewModel.kt
│   │   │   └── HomeAdapter.kt
│   │   ├── learn/
│   │   │   ├── LearnFragment.kt
│   │   │   ├── LearnViewModel.kt
│   │   │   └── WordCardAdapter.kt
│   │   ├── review/
│   │   ├── mistake/
│   │   ├── worddetail/
│   │   ├── stats/
│   │   ├── wordbook/
│   │   ├── settings/
│   │   └── onboarding/
│   ├── service/
│   │   ├── ReviewScheduler.kt
│   │   └── AudioPlayer.kt
│   ├── util/
│   │   ├── DateUtils.kt
│   │   └── StatsCalculator.kt
│   └── WordForgeApp.kt          (Application)
├── res/
│   ├── drawable/                 (图标、背景)
│   ├── values/
│   │   ├── colors.xml
│   │   ├── strings.xml
│   │   ├── themes.xml            (Material 3 主题)
│   │   └── dimens.xml
│   ├── values-night/
│   │   ├── colors.xml            (深色模式配色)
│   │   └── themes.xml
│   └── navigation/
│       └── nav_graph.xml
├── assets/
│   ├── wordbooks/                (内置词库JSON)
│   └── audio/                    (内置发音MP3，可选)
└── AndroidManifest.xml
```

---

> **文档结束**
> 
> 本设计方案覆盖了背单词APP的全部核心页面、交互行为、UI组件、动效、深浅模式适配、异常状态处理，以及完整的技术架构设计。所有设计均遵循 Material Design 3 规范，确保一致性和可访问性。
